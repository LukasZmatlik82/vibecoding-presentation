# Optional: shared feedback wall across all devices (Supabase free tier).
# Copy to ".env" and fill values from Supabase → Project Settings → API.
# Leave empty to use browser-only storage (localStorage).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
